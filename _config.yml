# GitHub Pages Jekyll Configuration for Clean URLs
title: "Creator Economy Convention"
description: "You are not a machine. Three days in Vancouver to rediscover what makes you human — and build a brand around it."

# Clean URLs - GitHub Pages will serve files without .html extension automatically
# For custom routing, see 404.html

# Build settings
markdown: kramdown
highlighter: rouge

# Exclude files
exclude:
  - .htaccess
  - README.md

# Include files
include:
  - _pages
